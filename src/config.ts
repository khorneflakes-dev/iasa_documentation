/**
 * Configuración global del sitio.
 *
 * AUTH_ENABLED:
 *   true  → El login con Cognito está activo. Los usuarios deben autenticarse para acceder.
 *   false → El login está desactivado. El sitio es accesible sin autenticación.
 */
export const AUTH_ENABLED = false;
